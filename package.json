{
  "name": "microsoft-login-counter",
  "version": "1.0.0",
  "description": "Browser extension to detect and count Microsoft authentication events at login.microsoftonline.com",
  "main": "dist/background/login-detector.js",
  "scripts": {
    "build": "node build.js",
    "build:firefox": "node build.js --firefox",
    "build:watch": "node build.js --watch",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "clean": "rm -rf dist/ dist-firefox/",
    "type-check": "tsc --noEmit",
    "lint": "echo 'Lint not configured yet'",
    "format": "echo 'Format not configured yet'"
  },
  "keywords": [
    "browser-extension",
    "microsoft",
    "authentication",
    "login-counter"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@jest/globals": "^29.7.0",
    "@types/chrome": "^0.0.268",
    "@types/jest": "^29.5.12",
    "@types/node": "^20.12.7",
    "esbuild": "^0.20.2",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "webextension-polyfill": "^0.11.0"
  }
}
