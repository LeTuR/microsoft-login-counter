[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "microsoft-login-counter"
version = "0.1.0"
description = "Proxy-based Microsoft login frequency tracker"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
